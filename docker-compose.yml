version: '3.8'

services:
  # Database Services
  postgres:
    image: postgres:14-alpine
    container_name: safa-postgres
    environment:
      POSTGRES_DB: safa_life
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_INITDB_ARGS: "--encoding=UTF-8 --lc-collate=C --lc-ctype=C"
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./scripts/init.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - safa-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d safa_life"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:6-alpine
    container_name: safa-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - safa-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Monitoring Services
  jaeger:
    image: jaegertracing/all-in-one:latest
    container_name: safa-jaeger
    ports:
      - "16686:16686"  # Jaeger UI
      - "14268:14268"  # HTTP collector
      - "14250:14250"  # gRPC collector
      - "4317:4317"    # OTLP gRPC receiver
      - "4318:4318"    # OTLP HTTP receiver
      - "6831:6831/udp"  # UDP agent
      - "6832:6832/udp"  # UDP agent
    environment:
      - COLLECTOR_OTLP_ENABLED=true
      - COLLECTOR_ZIPKIN_HOST_PORT=:9411
    networks:
      - safa-network
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:16686/"]
      interval: 30s
      timeout: 10s
      retries: 3

  prometheus:
    image: prom/prometheus:latest
    container_name: safa-prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
      - prometheus_data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.console.libraries=/etc/prometheus/console_libraries'
      - '--web.console.templates=/etc/prometheus/consoles'
      - '--storage.tsdb.retention.time=200h'
      - '--web.enable-lifecycle'
      - '--web.enable-remote-write-receiver'
    networks:
      - safa-network
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:9090/-/healthy"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Grafana (optional, for visualization)
  grafana:
    image: grafana/grafana:latest
    container_name: safa-grafana
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
      - GF_SECURITY_ADMIN_USER=admin
      - GF_USERS_ALLOW_SIGN_UP=false
      - GF_INSTALL_PLUGINS=grafana-piechart-panel
    volumes:
      - grafana-storage:/var/lib/grafana
      - ./grafana/provisioning:/etc/grafana/provisioning
    networks:
      - safa-network
    depends_on:
      prometheus:
        condition: service_healthy
      jaeger:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget --no-verbose --tries=1 --spider http://localhost:3000/api/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Application Service
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: safa-api
    ports:
      - "8080:8080"
    environment:
      # Database Configuration
      - SAFA_DB_HOST=postgres
      - SAFA_DB_USER=postgres
      - SAFA_DB_PASSWORD=password
      - SAFA_DB_NAME=safa_life
      - SAFA_DB_PORT=5432
      - SAFA_DB_SSL_MODE=disable
      - SAFA_DB_TIMEZONE=UTC
      
      # Redis Configuration
      - SAFA_REDIS_HOST=redis
      - SAFA_REDIS_PORT=6379
      - SAFA_REDIS_PASSWORD=
      - SAFA_REDIS_DB=0
      
      # Server Configuration
      - SAFA_SERVER_HOST=0.0.0.0
      - SAFA_SERVER_PORT=8080
      - SAFA_SERVER_ENV=development
      
      # Application Configuration
      - SAFA_APP_NAME=Safa Life API
      - SAFA_APP_VERSION=1.0.0
      
      # Monitoring Configuration
      - SAFA_MONITORING_JAEGER_ENDPOINT=jaeger:4318
      - SAFA_MONITORING_JAEGER_SERVICE_NAME=safa-life-api
      - SAFA_MONITORING_JAEGER_SERVICE_VERSION=1.0.0
      - SAFA_MONITORING_JAEGER_ENVIRONMENT=development
    volumes:
      - ./configs:/app/configs:ro
    networks:
      - safa-network
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      jaeger:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Optional: Database Admin Tool
  adminer:
    image: adminer:latest
    container_name: safa-adminer
    ports:
      - "8081:8080"
    environment:
      - ADMINER_DEFAULT_SERVER=postgres
    networks:
      - safa-network
    depends_on:
      - postgres
    profiles:
      - admin

  # Optional: Redis Admin Tool
  redis-commander:
    image: rediscommander/redis-commander:latest
    container_name: safa-redis-commander
    ports:
      - "8082:8081"
    environment:
      - REDIS_HOSTS=local:redis:6379
    networks:
      - safa-network
    depends_on:
      - redis
    profiles:
      - admin

volumes:
  postgres_data:
    driver: local
  redis_data:
    driver: local
  prometheus_data:
    driver: local
  grafana_data:
    driver: local
  grafana-storage:
    driver: local

networks:
  safa-network:
    driver: bridge
    ipam:
      config:
        - subnet: 172.20.0.0/16